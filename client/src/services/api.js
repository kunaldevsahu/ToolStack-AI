// API service for making HTTP requests
// Configure API endpoints and interceptors here

const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000';

export const api = {
  // Add API methods here
};
